## Getting Started

First, run the development server:

```bash
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

## Building

The build is what is actually deployed to users, so to update the frontend with the backend simple run

```bash
npm run build
```